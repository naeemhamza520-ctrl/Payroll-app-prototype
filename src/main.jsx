import React from "react";
import ReactDOM from "react-dom/client";
import PayrollApp from "./PayrollPrototype.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PayrollApp />
  </React.StrictMode>
);
